# Codex macOS 离线素材包

这是第三方整理的离线安装素材包，不是 OpenAI 官方发行物。官方 Codex 桌面客户端与 CC Switch 保留原签名。

本包仅支持 Apple Silicon Mac。解压后双击 `START-macOS.command`；如被系统拦截，右键该文件后选择“打开”。安装过程不从境外站点下载组件。

包内不含 API Key、Token 获取说明或给 AI Agent 的提示词。
