# Codex Windows Primary Runtime（配套包）

这是 Codex Windows Base 构建产物的配套离线 Runtime。请将解压后的本文件夹与 Base 文件夹放在同一目录，再从 Base 双击 `START-WINDOWS.cmd`。

如果单独运行 `INSTALL-RUNTIME.cmd`，脚本也会核对当前进程与桌面用户，不会把 Runtime 写到 SYSTEM 或另一个管理员账户。

本包不含 API Key、Token 获取说明或 Agent 提示词。
